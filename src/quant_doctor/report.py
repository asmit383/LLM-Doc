"""Report rendering — Phase 1.

Turns a diagnosis object into a CLI table (layer heatmap + verdict) or JSON.
"""

from __future__ import annotations

# Phase 1: render_table(diagnosis), render_json(diagnosis)
